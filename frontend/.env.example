# Production backend URL (no trailing slash).
# Set the SAME value as VITE_API_URL in the Vercel dashboard
# (Project → Settings → Environment Variables), then redeploy.
# Local dev does NOT need this: vite.config.ts proxies /api → http://localhost:4000.
VITE_API_URL=https://your-backend-host.example.com
